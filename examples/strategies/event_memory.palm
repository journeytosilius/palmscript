interval 1m
source spot = binance.spot("BTCUSDT")

let prior_high = highest(spot.close, 5)[1]
let breakout = above(spot.close, prior_high)

export last_breakout_close = valuewhen(breakout, spot.close, 0)
trigger breakout_now = breakout

plot(barssince(breakout))
